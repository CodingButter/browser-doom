# wi/ — Intermission

The stats screen shown between levels: kills, items, secrets, par time, plus the animated overlay traveling between map markers on the episode map.

## Expected files

| File | Ported from | Purpose |
|------|-------------|---------|
| `wi_stuff.c.ts` / `wi_stuff.h.ts` | `wi_stuff.{c,h}` | Intermission state machine, animations, number drawing |
